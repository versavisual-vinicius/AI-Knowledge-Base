# Claude Code

- Versão mínima conhecida: Claude Code com suporte a plugins ou `CLAUDE.md`.
- Integração: `.claude-plugin/plugin.json`, `CLAUDE.md` e `hooks/hooks.json`.
- Bootstrap: `CLAUDE.md` ou plugin local apontando para `BOOTSTRAP.md`.
- Fonte: `skills/`, `00_SYSTEM/` e `SPEC.md`; referências em `references/`.
- Instalação local: configurar o repositório como extensão/plugin local.
- Validação: iniciar uma sessão e verificar consulta de contexto antes da ação.
- Limitação atual: adapter ainda não foi testado no Claude Code real.
- Estado: Draft.
